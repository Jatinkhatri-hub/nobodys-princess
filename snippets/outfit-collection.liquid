{% if heading != blank %}
    <div class="outfit-collection__header jusfify-self-{{ text-alignment |  default: 'start'}} text-{{ text-alignment |  default: 'start' }}">
    {% if heading != blank %} 
        <div class="outfit">
        <div>
        {% if heading != blank %}
            <h4 class="h4">{{ heading }}</h4>
        {% endif %}
        </div>
        </div>
    {% endif %}
    </div>
{% endif %}